1. Copy the folder and myemail_helper.php to the helper folder 
2. Load helper "myemail"
3. use function  myEmail(someone_address,someone_name,email_title,email_content)
4. myEmail() function return true if it works and return false if it not work